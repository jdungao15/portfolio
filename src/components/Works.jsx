import React from 'react';
import styled from "styled-components";

const Section = styled.div`
  height: 100vh;
  background-color: rgba(193, 83, 83, 0.62);
  scroll-snap-align:center;
`

const Works = () => {
   return (
      <Section>
         <h1>Works</h1>
      </Section>
   );
};

export default Works;
